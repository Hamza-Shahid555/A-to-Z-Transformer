Layer Normalization (LayerNorm) is a technique used in Transformer models (GPT, BERT, LLaMA, etc.) to 
stabilize training by normalizing 
the activations of each token independently across its feature dimension.
