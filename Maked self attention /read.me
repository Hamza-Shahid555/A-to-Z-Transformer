Maked self attention core idea

👉 “A word can only see past words — not future ones.”

Example: Sentence: “I love AI” While predicting “love”, model cannot see “AI”

⚡ Why It Matters Prevents cheating during training Enables correct text generation (left → right) Used in autoregressive models (GPT)

👉 Without masking → model would see future → wrong learning
