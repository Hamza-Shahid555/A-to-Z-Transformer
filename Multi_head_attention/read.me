What is Multi-head Attention?
At its core, attention asks: "For each word in a sentence, how much should I focus on every other word?"
Multi-head attention runs this process multiple times in parallel,
each with a different "perspective" (head) — one head might focus on syntax,
another on semantics, another on coreference.
